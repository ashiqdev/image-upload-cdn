# import * from './generated/prisma.graphql'
type Mutation {
  createImage(original:String!, w200:String!): Image!
}

type Query {
  getImages(where: ImageWhereInput, orderBy: ImageOrderByInput, skip: Int, after: String, before: String, first: Int, last: Int): [Image]!
  getImage(id: ID!): Image!
}
